# 연구노트 2026-08-23

| | |
|---|---|
| **단계** | 학습 최적화 |
| **주제** | 업스트림 조사 결과 적용 — 학습 고속경로 및 보상 사전점검 |
| **관련 커밋** | `52326a9` |

## 1. 목적

- Unsloth·GRPO 관련 최신 저장소·스킬을 조사해 본 과제에 적용 가능한 것을 찾는다.

## 2. 수행 및 결과

### 2.1 Unsloth 고속경로 진입 조건 미충족 발견

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| `lora_dropout` | 0.05 | **0.0** |
| `bias` | 기본값 | **"none"** |

- 위 두 조건을 만족하지 않으면 Unsloth 최적화 커널을 타지 못한다.
- 재발 방지를 위해 설정 검증 함수 `validate()`를 추가하고 경고를 출력하도록 했다.

### 2.2 GRPO 전환 대비

- **보상함수 사전 점검 게이트** 신설 — 보상 해킹을 조기 탐지한다.
- 파라미터 확정: `num_generations=8`, `temperature=0.8`, `beta=0.04`
- vLLM 서버 모드 / colocate 모드 비교 검토.

### 2.3 인용검증 개선 효과

- 오탐 증가 없이 재현율만 상승 — 규정위반 탐지 KPI(90%+)에 직접 기여.
- `fake_doi` / `no_source` / `numeric_tamper` 탐지 100% (합성 기준).

## 3. 정정 사항

### 정정 05 — 학습 설정

LoRA dropout이 0이 아니면 고속경로를 벗어난다. **설정 한 줄이 학습 속도 전체를 좌우**하므로, 이후 모든 학습 설정은 검증 함수를 통과해야 한다.

## 4. 판단

- 합성 기준 100%는 회귀 테스트 값이다. 실데이터 기준선(SciFact F1 24.0%)과 병기해야 의미가 있다.

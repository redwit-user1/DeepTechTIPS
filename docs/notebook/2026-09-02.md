# 연구노트 2026-09-02

| | |
|---|---|
| **단계** | 분류기 검증 |
| **주제** | 분류기 일치도 측정, 전 구간 재판정, 혼동행렬 역산 보정 |
| **관련 커밋** | (본 커밋) |

## 1. 목적

두 가지 미해결 항목을 수행한다.

1. **분류기 정확도 검증** — 지금까지의 모든 분류 수치가 정확도 미검증 상태다.
2. **버킷·분야 전 구간 재판정** — 기존 수치가 앞부분 연속 60,000행 표본이라 구간 편향 가능성이 있다.

## 2. 수행 ① 분류기 일치도

### 2.1 방법과 그 한계

정규식 분류기와 **독립적인 2차 판정**을 180건 수행했다.
**사람 검수가 아니므로 "정확도"가 아니라 "일치도"다.**
`label`(사람 몫) 칸은 비워 두고 `model_label` 칸에 기록했다.

### 2.2 결과

| 판정 단위 | 표본 일치도 | 코퍼스 가중 |
|---|---|---|
| 10개 버킷 | 48.9% | **43.1%** |
| 3대분류(연구/GitHub/기타) | 76.1% | **69.5%** |

| 대분류 | 재현율 | 정밀도 |
|---|---|---|
| 연구내용 | 78.7% | 87.6% |
| GitHub | 78.8% | 95.3% |
| 기타(조각·행정) | 55.0% | **27.5%** |

`fragment` 단독 정밀도는 **9.4%** — 조각이라 판정한 것 중 실제 조각은 10건 중 1건이다.

주요 혼동 (180건 중):

```
연구내용 → 기타            22건   ← 한 방향으로 쏠림
기타   → 연구내용           8건
code_commit → code_meta   9건
experimental ↔ analysis  12건
```

### 2.3 판정

- **세부 버킷(10종) 수치는 철회한다.** 43.1%는 사용 불가.
- **대분류는 방향만 유효하다.** 오분류가 연구→기타 한 방향으로 쏠려 있고
  정밀도(87.6%)가 재현율(78.7%)보다 높으므로, **연구 내용은 46.5%보다 많다.**
- GitHub 정밀도 95.3%는 양호하다. 다만 하위 4종 구분은 무의미하다.

## 3. 수행 ② 전 구간 재판정

앞부분 편향을 없애기 위해 파일 전체를 순회하며 20행마다 판정했다(282,717행).

| | 앞부분 60,000행 | **전 구간 282,717행** | 괴리 |
|---|---|---|---|
| 연구 내용 | 46.54% | 46.48% | 0.06p |
| GitHub | 14.59% | 14.49% | 0.10p |
| fragment | 38.36% | 38.53% | 0.17p |
| 생명·보건의료 | 22.67% | 22.84% | 0.17p |

**최대 괴리 0.2p.** 앞부분 표본은 전 구간을 잘 대표했다.

### 판정

**구간 편향은 없었다. 문제는 표본 위치가 아니라 분류기였다.**
이 결과로 "앞부분 6만 행이라 못 믿는다"는 그동안의 유보를 해제한다.

## 4. 혼동행렬 역산 보정

두 측정이 서로 다른 것을 알려주므로 합칠 수 있다.

- 전 구간 판정 → 분류기의 예측 분포 `P(pred)` (구간 편향 없음이 확인됨)
- 라벨셋 → 예측 버킷별 실제 분포 `P(gold | pred)` (예측 버킷 기준 층화 추출이므로 편향 없음)

```
P(gold=g) = Σ_p  P(gold=g | pred=p) · P(pred=p)
```

분류기가 **일관되게** 틀리면 그 틀림 자체를 보정할 수 있다. 계통 오차이지 노이즈가 아니기 때문이다.

### 결과 (3대분류)

| | 관측 | 보정 | 변화 |
|---|---|---|---|
| 연구 내용 | 46.48% | **65.58%** (±7.65) | +19.1p |
| GitHub | 14.49% | **23.48%** (±6.15) | +9.0p |
| 기타 | 39.03% | **10.94%** (±6.09) | −28.1p |

## 5. 이 보정을 어디까지 믿을 것인가

### 레버리지 문제 — 10개 버킷 보정은 쓸 수 없다

층화 추출이 버킷마다 비슷한 수를 뽑았는데, **코퍼스 질량은 전혀 균등하지 않다.**
그래서 판정 1건이 전체 비율을 움직이는 폭이 버킷마다 20배 차이 난다.

| 예측 버킷 | 코퍼스 질량 | 판정 표본 | 1건당 영향 |
|---|---|---|---|
| fragment | 38.53% | 32건 | **1.20p** |
| experimental | 33.17% | 30건 | **1.11p** |
| code_meta | 7.08% | 10건 | 0.71p |
| analysis | 9.66% | 27건 | 0.36p |
| literature | 2.13% | 23건 | 0.09p |
| admin | 0.50% | 8건 | 0.06p |

**상위 2개 버킷이 코퍼스의 71.7%를 차지하는데 판정은 62건뿐이다.**

`planning 1.52% → 16.05%` 같은 값은 fragment 표본 32건 중 7건이 계획으로
판정된 결과이며, 7건이 8.4 퍼센트포인트를 움직였다. **너무 취약해 쓸 수 없다.**

### 표본 설계의 오류

| 목적 | 필요한 표본 설계 |
|---|---|
| 버킷별 정확도 측정 | 버킷 **균등** 층화 ← 현재 설계 |
| 전체 비율 보정 | 예측 **질량 비례** |

두 목적이 서로 다른 설계를 요구하는데 하나만 만들었다.

## 6. 정정 사항

### 정정 16 — 세부 버킷 비율 철회

실험 33.2% / 분석 9.6% / 문헌 2.2% / 계획 1.5%, 그리고 분야 분포(생명 22.7% 외)를
**철회한다.** 10개 버킷 일치도가 43.1%이고, 분야 분류는 그 위에 얹힌 값이다.

### 정정 17 — 구간 편향 유보 해제

"앞부분 연속 6만 행이라 대표성이 없을 수 있다"는 유보를 **해제한다.**
전 구간 재판정 결과 최대 괴리 0.2p로, 표본 위치는 문제가 아니었다.

### 정정 18 — CSV 배너 파싱 결함

`build_label_set.py`가 쓴 배너를 `#` 시작 줄 필터로 걷어냈는데,
인용 필드 **안**의 본문 줄이 `#`로 시작하면 그것까지 지워져 CSV가 깨졌다.
헤더 앞부분만 걷어내도록 수정.

## 7. 후속 영향

| 대상 | 영향 |
|---|---|
| 근거·주장 쌍 계획 | **직접 타격.** 실험/분석 구분에 의존하는데 그 둘 혼동이 180건 중 12건 |
| 논문 혼입 11.4% | 영향 없음 — 별도 표지 기반 |
| 전수 물성(중복 23%, 한글 14.2%, OCR 0.08%) | 영향 없음 |

## 8. 차기 계획

1. **fragment·experimental 버킷에 라벨 집중** — 각 200건 이상.
   두 버킷이 코퍼스의 71.7%를 쥐고 있다. 균등 층화가 아니라 질량 비례로 뽑는다.
2. **사람 검수** — 현재 판정은 모델 2차 판정이다. 보정값은 그것을 정답으로 가정한 결과다.
3. **분류기 수정** — `fragment` 정밀도 9.4%가 최대 결함. 조각 판정을 대폭 좁혀야 한다.
4. 근거·주장 쌍 생성은 3번 이후로 미룬다.

## 9. 한 줄

**비율은 안정적인데 라벨이 틀렸다.** 분류기가 일관되게 틀리므로 보정은 가능하지만,
질량이 큰 두 버킷의 표본이 62건뿐이라 보정값 자체가 아직 불안정하다.

---

# [추가] 무작위 표본 + 분류기 수정 3회 반복

## 10. 표본 설계 교체 — 층화에서 무작위로

층화 표본으로 비율을 보정하려다 레버리지 문제에 부딪혔으므로(5절),
**전 구간 단순 무작위 표본 360건**을 새로 뽑았다.

무작위 표본은 자동으로 질량 비례이므로 **혼동행렬 역산이 필요 없다.**
`P(gold)` 가 직접 나오고 표준오차도 `sqrt(p(1-p)/n)` 로 단순하다.

| 표본 | 설계 | 용도 |
|---|---|---|
| 1차 500건 | 버킷 균등 층화 | 버킷별 재현율·정밀도 |
| **2차 360건** | **전 구간 무작위** | **코퍼스 비율** |

표본 건전성 확인: 예측 분포가 전 구간 재판정치와 일치했다(fragment 38.61% vs 38.53%).

## 11. 실측 비율 (무작위 표본 170건 판정)

| 버킷 | 분류기 예측 | **실제** | 편차 |
|---|---|---|---|
| **experimental** | 34.1% | **32.9%** | **정확** |
| fragment | 41.2% | 17.1% | 2.4배 과대 |
| planning | 2.4% | 11.8% | 5.0배 과소 |
| literature | 1.8% | 10.6% | 6.0배 과소 |
| code_commit | 2.4% | 10.6% | 4.5배 과소 |
| admin | 0.0% | 5.9% | 미작동 |
| analysis | 9.4% | 5.3% | 1.8배 과대 |
| code_docs | 0.0% | 3.5% | 미작동 |
| code_meta | 5.3% | 0.6% | 9.0배 과대 |

**3대분류:** 연구내용 예측 47.6% → **실제 60.6% (±7.3)**

- 혼동행렬 역산 보정치(65.58%)와 **독립적으로 일치**한다. 두 방법이 서로를 검증했다.
- **실험 기록 조건만은 정확하다**(34.1% vs 32.9%). 이 조건은 건드리지 않는다.

## 12. 결정적 결함 — GOONO 커밋 형식

`_COMMIT` 패턴이 `commit <해시>` 만 봤는데, **GOONO 내보내기는 `commit <ISO 타임스탬프>` 형식**이고 해시는 URL 끝(`/git/commits/<hash>`)에 붙는다.

```
commit 2024-12-09T08:26:22.000Z [author] - message /
https://api.github.com/repos/X/Y/git/commits/d00c2628...
```

→ commit 신호 **0**. 커밋 목록이 통째로 `code_meta` 나 `fragment` 로 밀렸다.

추가로 `code_subtype` 이 "나머지는 전부 code_meta" 였다. 설정 파일의 **양성 표지**(`_CONFIG`)를 요구하도록 바꿨다.

## 13. 수정 3회 반복 결과

같은 170건 판정으로 세 버전을 채점했다.

| | 원본 | v2 커밋 수정 | v3 임계값 수정 |
|---|---|---|---|
| 10버킷 일치도 | 47.6% | 55.9% | **58.2%** |
| 3대분류 일치도 | 68.8% | 71.8% | **74.1%** |
| 연구내용 오차 | +12.9p | +14.1p | **+8.2p** |
| GitHub 오차 | +5.3p | **+2.4p** | +2.4p |
| 기타 오차 | −18.2p | −16.5p | **−10.6p** |

- **v2:** `code_commit` 예측 2.4% → 10.6%, 실제 10.6%로 **정확히 일치**. `code_meta` 5.3% → 0%.
- **v3:** 문헌·계획 임계값 3→2, 흩어진 신호 구제 3→2, 행정 3→2.

## 14. 여기서 멈추는 이유 — 과적합

**같은 170건으로 튜닝하고 같은 170건으로 채점했다.** train-on-test 이므로
v3 수치는 낙관적이다. 더 조정하면 내 판정의 노이즈에 맞추게 된다.

- 표본 360건 중 **190건은 아직 판정하지 않았다.** 이것이 홀드아웃 셋이다.
- 게다가 gold 가 사람 검수가 아니라 **모델 2차 판정**이다.

## 15. 남은 결함

| 결함 | 현황 |
|---|---|
| fragment 2.0배 과대 | 여전히 최대 결함 |
| planning·literature 3.3~3.6배 과소 | 개선됐으나 남음 |
| admin·code_docs 미작동 | 예측 0%, 실제 5.9%·3.5% |

## 16. 차기 계획 (갱신)

1. 홀드아웃 190건 판정 → v3 의 과적합 정도 확인
2. 사람 검수 — 현재 gold 는 모델 판정이다
3. admin·code_docs 경로 신설
4. 근거·주장 쌍은 experimental/analysis 구분이 안정된 뒤

## 17. 잠정 수치 (사람 검수 전)

**연구 내용 60.6% ± 7.3** → 약 342만 행 (기존 발표치 46.5% / 263만 행)
